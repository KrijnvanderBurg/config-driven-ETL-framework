name: Release Python App

on:
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  build-and-test:
    uses: ./.github/workflows/build-and-test.yml

  release:
    needs: build-and-test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Display version
        run: echo "Version to release is ${{ needs.build-and-test.outputs.version }}"

      - name: Download Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      # Add release steps here
      # Use ${{ needs.build-and-test.outputs.version }} to access the version
